require 'spec_helper'

describe Wit::Session::Client do
  it "should create a new client instance given some auth token" do
    pending("Need to instantiate the given client with a given token.")
  end

  it "should be able to respond and spit out the current instance auth token" do
    pending("Set up instance and call the auth_token return method")
  end

  it "should set up a default ssl connection" do
    pending("Set up connection with random auth token")
  end

end
